import { router, usePage } from "@inertiajs/react";
import { Check, ChevronRight, Home, ShoppingBag } from "lucide-react";
import SidebarJSE from "../Composants/Navigation/SidebarJSE";

export default function CommandeConfirmation(){
 const { commande }=usePage().props;
 const prix=(v)=>new Intl.NumberFormat("fr-FR",{maximumFractionDigits:0}).format(Number(v||0));
 return <main className="min-h-screen bg-jse-fond text-jse-texte"><div className="mx-auto flex min-h-screen w-full max-w-none"><SidebarJSE /><div className="min-w-0 flex-1"><div className="mx-auto flex min-h-screen w-full max-w-none items-center px-5 py-10"><section className="w-full rounded-[32px] bg-white p-7 text-center shadow-xl ring-1 ring-jse-texte/5 sm:p-10"><div className="mx-auto flex size-20 items-center justify-center rounded-full bg-jse-secondaire text-white"><Check size={38}/></div><p className="mt-7 font-sans text-xs font-bold uppercase tracking-[0.16em] text-jse-secondaire">Commande créée</p><h1 className="mt-2 font-against text-[2.7rem] leading-none text-jse-principal">Merci pour votre commande</h1><p className="mt-3 font-sans text-sm leading-6 text-jse-texte/55">Votre commande <strong className="text-jse-principal">{commande.reference}</strong> est enregistrée et attend sa prise en charge.</p><div className="mt-7 rounded-[22px] bg-jse-fond p-5"><div className="flex justify-between font-sans text-sm"><span>Total</span><strong className="text-jse-principal">{prix(commande.montant_total)} FCFA</strong></div><div className="mt-3 flex justify-between font-sans text-sm"><span>Restaurant</span><strong>{commande.restaurant_nom}</strong></div></div><div className="mt-7 grid gap-3 sm:grid-cols-2"><button type="button" onClick={()=>router.visit("/accueil")} className="flex h-13 items-center justify-center gap-2 rounded-full bg-jse-fond px-5 py-3 font-sans text-sm font-bold text-jse-principal"><Home size={18}/>Accueil</button><button type="button" onClick={()=>router.visit("/commandes")} className="flex items-center justify-center gap-2 rounded-full bg-jse-principal px-5 py-3 font-sans text-sm font-bold text-white"><ShoppingBag size={18}/>Voir mes commandes</button></div></section></div></div></div></main>
}